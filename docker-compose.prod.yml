version: '3.8'

services:
  backend-prod:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hospital-backend-prod-cloud
    ports:
      - "8020:8080"
    environment:
      - QUARKUS_PROFILE=prod
      - DB_HOST=35.208.14.178
      - DB_PORT=1523
      - DB_NAME=XE
      - DB_USER=C##HOSPITAL
      - DB_PASSWORD=hospital123
    restart: unless-stopped

  frontend-prod:
    build:
      context: .
      dockerfile: Dockerfile.frontend.prod
    container_name: hospital-frontend-prod-cloud
    ports:
      - "8021:80"
    environment:
      - NODE_ENV=production
      - VITE_API_URL=http://34.61.228.49:8020
    restart: unless-stopped